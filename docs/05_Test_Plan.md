  

### Классификация и общие механизмы

 

✅  **Тест 1.** "Привет" → fallback-ответ.
✅  **Тест 2.** Вопрос по регламенту, покрытый документами → содержательный ответ.
✅  **Тест 3.** Вопрос по регламенту, не покрытый документами → "информация не найдена", без выдуманных цифр.
✅  **Тест 4.** Команда "Новый запрос" вне какого-либо сценария → приветственный ответ, без ошибок.
✅  **Тест 5.** Команда "Отмена" посреди уточнения (например, во время ввода города для reserve) → полный сброс состояния, приветственный ответ.

 

### my_bookings (без изменений структурно)

 

✅ **Тест 6.** "Какие у меня брони?" → уточнение ФИО.
✅ **Тест 7.** "Покажи брони Иванов И.И." → список активных броней.
✅ **Тест 8.** "Покажи брони Несуществующий Сотрудник" → "активных броней нет".
✅ **Тест 9.** "Покажи брони Иванов" (без инициалов) → пустой список.
✅ **Тест 10.** Двухшаговый: "Мои брони" → "Иванов И.И." → список после уточнения (проверка merge/памяти).

 

### Reserve — полный цикл с подтверждением

 

✅ **Тест 11.** Полный happy path: "Хочу машину в Уфе на 22.09.2026" → выбрать модель → "Для Смирнова А.П." → экран подтверждения → "Да". Ожидаемо: реальная бронь создана.
✅ **Тест 12.** Подтверждение "Нет" → "Прервать": дойти до подтверждения → "Нет" → "Прервать". Ожидаемо: бронь не создана, все conv-переменные (кроме employee_name) очищены единым блоком.
✅ **Тест 13.** Подтверждение "Нет" → "Заменить": дойти до подтверждения → "Нет" → "Заменить" → указать новую дату. Ожидаемо: город/модель/ФИО сохранились, новый экран подтверждения с обновлённой датой.
 ✅ **Тест 14.** Неоднозначный ответ на "Да/Нет" (например, "не уверен") → повторный показ вопроса подтверждения, без продвижения.

 ✅ **Тест 15.** Неоднозначный ответ на "Прервать/Заменить" → повторный показ этого вопроса.
 ✅ **Тест 16.** Команда "Новый запрос" на экране подтверждения (вместо Да/Нет) → полный сброс, приветственный ответ.
 ✅ **Тест 17.** Отказ — нет свободных машин модели (Toyota Camry, Уфа, 14.08.2026, все заняты) → отказ до экрана подтверждения (подтверждать нечего, т.к. reserve_available_car ещё не вызывался на этом этапе — сверить, что отказ формулируется на этапе поиска модели, а не после лишнего "Да").
✅  **Тест 18.** Отказ — сотрудник не найден → аналогично, корректная формулировка.
✅ **Тест 19.** Отказ — бизнес-правило "уже есть бронь в этом городе на дату" → бронирование второй машины тем же сотрудником в тот же город/дату после первой успешной брони (тест 11) — отказ с соответствующим сообщением.
✅  **Тест 20.** Все параметры одним сообщением: "Забронируй Kia Rio в Москве на 10.08.2026 для Смирнова А.П." → сразу экран подтверждения (без промежуточных уточнений) → "Да" → бронь создана.
✅ **Тест 21.** Относительная дата: "Хочу машину в Санкт-Петербурге на завтра" → корректная нормализация даты, доходит до подтверждения.
✅ **Тест 22.** Уточнение ФИО подхватывается после ответа на "Прервать/Заменить" цикл — двухшаговый: подтверждение → "Нет" → "Заменить" → указать новое ФИО вместо даты/города — merge корректно обновляет только ФИО.

 

### Cancel — полный цикл с подтверждением

 

✅ **Тест 23.** Happy path: "Отмени бронь Volkswagen Polo для Иванов И.И." → находит одну бронь → экран подтверждения → "Да" → бронь реально отменена.
✅ **Тест 24.** Подтверждение "Нет" → "Прервать" → бронь остаётся активной, состояние очищено.
✅ **Тест 25.** Подтверждение "Нет" → "Заменить" → указать другую бронь для отмены (другой город) → новый поиск, новый экран подтверждения с другими данными.
 ✅ **Тест 26.** Найдено 0 броней → уточняющий ответ (без подтверждения, т.к. подтверждать нечего) — сверить, что conv_cancel_\* не сбрасываются автоматически (см. решение из более раннего разговора).
 ✅ **Тест 27.** Найдено больше 1 брони → уточняющий вопрос по модели/городу/дате → доуточнение → однозначная бронь → экран подтверждения → "Да" → отмена.
 ✅ **Тест 28.** Повторная отмена уже отменённой брони (сразу после теста 23) → "не найдено", без ошибки.
✅  **Тест 29.** Неоднозначный ответ на подтверждение cancel → повторный показ вопроса.
 **Тест 30.** 

```javascript
conv_pending_booking_id
```

 корректно используется при отмене (не берётся из старого/чужого состояния) — специально проверить после сценария, где до этого был прерванный reserve (переход reserve→cancel, тест 33 из старого списка).

 

### Переключение между ветками и защита состояния

 

**Тест 31.** Reserve, дойти до уточнения даты → ответить "10 августа" → корректно распознаётся как продолжение reserve, не уходит в other/regulation.
 **Тест 32.** Cancel, дойти до уточнения (несколько броней) → ответить данными, совпадающими с booking-специфичными словами reserve (модель/город/дата) → корректно остаётся в cancel благодаря 

```javascript
conv_awaiting_clarification
```

 (регрессия на баг, найденный ранее).
 **Тест 33.** Прерванный reserve не влияет на последующий cancel: тест 12 (прервать reserve) → сразу "Отмени бронь ... для Иванов И.И." → cancel работает на чистом контексте.
 **Тест 34.** Явный regulation-вопрос посреди незавершённого reserve-диалога (после уточнения города, до подтверждения) → корректная классификация как regulation, не перехватывается booking-контекстом (известный компромисс из ADR — проверить актуальное поведение).

 

### Обработка сбоев инфраструктуры

 

**Тест 35.** Сбой get_available_models → 

```javascript
INFRA_ERROR_MESSAGE
```

, без падения workflow.
 **Тест 36.** Сбой reserve_available_car (на этапе после подтверждения "Да") → 

```javascript
INFRA_ERROR_MESSAGE
```

, бронь не создаётся.
 **Тест 37.** Сбой get_user_active_bookings → 

```javascript
INFRA_ERROR_MESSAGE
```

.
 **Тест 38.** Сбой find_booking_to_cancel → 

```javascript
INFRA_ERROR_MESSAGE
```

.
 **Тест 39.** Сбой cancel_booking (после подтверждения) → 

```javascript
INFRA_ERROR_MESSAGE
```

, бронь остаётся активной.

 

### Аудит-лог

 

**Тест 40.** После успешного reserve (тест 11) → проверить в audit_log запись 

```javascript
booking_created
```

 (через триггер) с корректными данными.
 **Тест 41.** После успешного cancel (тест 23) → проверить запись 

```javascript
booking_status_changed
```

 (old_status=confirmed, new_status=cancelled).
 **Тест 42.** После get_available_models (тест 11, начальный поиск) → проверить запись 

```javascript
search_available_models
```

 с 

```javascript
success: true
```

 и корректным 

```javascript
result_count
```

.
 **Тест 43.** После get_user_active_bookings (тест 7) → проверить запись 

```javascript
search_own_bookings
```

.
 **Тест 44.** После find_booking_to_cancel (тест 23) → проверить запись 

```javascript
search_booking_to_cancel
```

.
 **Тест 45.** При сбое get_available_models (тест 35) → проверить запись с 

```javascript
success: false
```

 и текстом ошибки в audit_log.
 **Тест 46.** Проверить 

```javascript
actor
```

 во всех записях равен 

```javascript
'HR_test'
```

 во всех типах событий.

 

### RLS

 

**Тест 47.** Запрос к 

```javascript
cars
```

/

```javascript
users
```

/

```javascript
bookings
```

/

```javascript
audit_log
```

 через 

```javascript
anon
```

\-роль (SET ROLE anon) → 0 строк на каждой таблице.
 **Тест 48.** Тот же набор через 

```javascript
authenticated
```

\-роль → 0 строк.
 **Тест 49.** Бот продолжает работать в полном объёме через 

```javascript
service_role
```

 (общая регрессия после включения RLS — по сути объединяет с тестами 11-30).

 

### Бизнес-правила и защита от гонки

 

**Тест 50.** Попытка забронировать вторую машину (любую модель) тем же сотрудником в тот же город/дату — отказ по бизнес-правилу (частично дублирует тест 19, зафиксировать отдельно как регрессию бизнес-правила независимо от конкретной модели).
 **Тест 51.** (Опционально, сложнее в постановке без спец. инструментов) — параллельный вызов 

```javascript
reserve_available_car
```

 дважды почти одновременно на одну модель/город/дату разными сотрудниками — оба должны получить разные машины или один — корректный отказ "нет свободных", без дублирования брони на одну машину.

 

### Обслуживание данных

 

**Тест 52.** 

```javascript
expire_past_bookings
```

 — вручную создать бронь с датой в прошлом, вызвать функцию, проверить переход в 

```javascript
cancelled
```

.
 **Тест 53.** Проверить регистрацию 

```javascript
pg_cron
```

 задания (

```javascript
SELECT * FROM cron.job
```

) — активно, корректное расписание.

  
